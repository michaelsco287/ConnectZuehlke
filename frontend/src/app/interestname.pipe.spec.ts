import {InterestnamePipe} from './interestname.pipe';

describe('InterestnamePipe', () => {
  it('create an instance', () => {
    const pipe = new InterestnamePipe();
    expect(pipe).toBeTruthy();
  });
});
