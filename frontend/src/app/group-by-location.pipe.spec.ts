import {GroupByLocationPipe} from './group-by-location.pipe';

describe('GroupByLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupByLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
